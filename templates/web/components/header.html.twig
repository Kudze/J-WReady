<nav class="navbar navbar-expand-lg navbar-light bg-white pt-3 pb-3 pl-5 pr-5">
    <a class="navbar-brand" href="{{ path("home") }}">J&amp;WReady</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto ml-auto">
            <li class="nav-item {{ "home" | activeHeaderCheck }}">
                <a class="nav-link" href="{{ path("home") }}">Home</a>
            </li>
            <li class="nav-item {{ "catalogue" | activeHeaderCheck }}">
                <a class="nav-link" href="{{ path("catalogue") }}">Catalogue</a>
            </li>
        </ul>
        <ul class="navbar-nav">
            <li class="nav-item {{ ["login", "register"] | activeHeadersCheck }}">
                <a class="nav-link" href="{{ path("login") }}">Sign In</a>
            </li>
        </ul>
    </div>
</nav>